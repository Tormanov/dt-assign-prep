<main>
    <section id="details-info">
        <h1>Details</h1>
        <div class="coin-image">
            <img src="{{crypto.image}}">
        </div>

        <div class="coin-info">
            <div class="coin-text">
                <h1 id="name">{{employee.name}}</h1>
                <h3 id="payment">Payment method: {{employee.paymentMethod}}</h3>
                <p id="price"><span>Price: ${{employee.price}}</span></p>
                <p id="description">{{employee.description}}</p>
            </div>
            <div class="product-btn">
                <div class="author">
                    <a href="/employees/{{employee._id}}/edit" class="btn-edit">Edit</a>
                    <a href="/employees/{{employee._id}}/delete" class="btn-delete">Delete</a>
                </div>

            </div>
        </div>
    </section>
</main>